# JalNiti backend dependencies.
# Pinned loosely (>=) on purpose: this is a teaching codebase, not a
# reproducible production deploy. Pin exactly before your final demo.

# --- Web API (step f) ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0

# --- Database (PostGIS) ---
SQLAlchemy>=2.0
GeoAlchemy2>=0.15
psycopg[binary]>=3.2

# --- Geospatial processing (steps a + b) ---
geopandas>=1.0
shapely>=2.0
rasterio>=1.3
osmnx>=2.0          # OSM roads + waterways
pyproj>=3.6
networkx>=3.3

# --- Data sources ---
earthengine-api>=1.1   # SRTM 30m DEM  (needs `earthengine authenticate`)
imddaily>=0.1.4        # IMD 0.25deg gridded historical rainfall parser
requests>=2.32         # IMD live/nowcast API at api.imd.gov.in
xarray>=2024.6         # reading NetCDF rainfall
netCDF4>=1.7

# --- Modelling (step c) -- deliberately NOT deep learning ---
scikit-learn>=1.5
pandas>=2.2
numpy>=1.26
joblib>=1.4

# --- Optimization (step d) -- a real solver, not a greedy heuristic ---
ortools>=9.10

# --- Config + dev ---
PyYAML>=6.0
pytest>=8.3
